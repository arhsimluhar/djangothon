============
Installation
============

At the command line::

    $ easy_install dj-dash-repo

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dj-dash-repo
    $ pip install dj-dash-repo
